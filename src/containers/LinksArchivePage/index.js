import React from 'react';
import { connect } from 'react-redux';

export const LinksArchivePage = () => (
  <div>
    <div>Links Archive Page</div>

  </div>
);

LinksArchivePage.propTypes = {

};

const mapStateToProps = () => ({

});

export default connect(mapStateToProps)(LinksArchivePage);
