import expect from 'expect';
// import { func } from 'redux-base/selectors/shortener';

describe('selectors for shortener', () => {
  const globalState = {};

  describe('selector', () => {
    it('should return state slice with ...', () => {
      // const actual = func(globalState);
      // const expected = globalState.something;

      // expect(actual).toEqual(expected);
    });
  });

});